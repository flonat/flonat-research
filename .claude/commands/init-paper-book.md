---
description: Use when you need to scaffold a NEW educational companion book for a
  LaTeX paper. Reads the paper, drafts 8 substantive chapters into the vault at ~/vault/books/{slug}/,
  copies bib + figures, registers the book, and verifies atlas serves it. Source-of-truth
  is the paper PDF/tex; the book is a reading companion, never a re-statement of new
  claims. For syncing an existing book to a paper revision, use audit-paper-book.
---

# Shared skill adapter

Read and follow `~/.claude/shared-skills/init-paper-book/SKILL.md`. Resolve bundled resources relative to `~/.claude/shared-skills/init-paper-book/`.
